# Copy to /etc/oauth2-proxy/oauth2-proxy.env on EC2 and fill in.
# Generate cookie secret with: openssl rand -base64 32 | tr -d /+= | head -c 32

OAUTH2_PROXY_CLIENT_ID=
OAUTH2_PROXY_CLIENT_SECRET=
OAUTH2_PROXY_COOKIE_SECRET=
